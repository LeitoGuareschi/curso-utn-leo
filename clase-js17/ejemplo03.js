const letras = ['a', 'b', 'c'];

const letras2 = ['n', 'm', 't'];

const letrastotales = letras.concat(letras2);

console.log(letras)
console.log(letras2)
console.log(letrastotales)

//contact() : Se utiliza para unir dos o mas arrays. Este metodo no cambia los arrays existentes , sino que devuelve un nuevo array.